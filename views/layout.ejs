<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- SEO Meta Tags -->
    <% // Default SEO values const seoTitle=typeof title !=='undefined' ? title
        : 'EduShare Connect - Peer-to-Peer Educational Platform' ; const seoDescription=typeof description
        !=='undefined' ? description
        : 'Join EduShare Connect to share and discover educational resources, connect with learners worldwide, and enhance your learning journey with courses, forums, and AI assistance.'
        ; const seoKeywords=typeof keywords !=='undefined' ? keywords
        : 'education, learning, resources, courses, study materials, peer learning, online education, EduShare Connect, educational platform, study tools'
        ; const seoImage=typeof ogImage !=='undefined' ? ogImage : '/images/og-image.png' ; const
        baseUrl=process.env.SITE_URL || 'https://edushare-connect.onrender.com' ; const canonicalUrl=typeof canonical
        !=='undefined' ? canonical : baseUrl + (typeof currentPath !=='undefined' ? currentPath : '' ); %>

        <title>
            <%= seoTitle %>
        </title>
        <meta name="description" content="<%= seoDescription %>">
        <meta name="keywords" content="<%= seoKeywords %>">
        <meta name="author" content="EduShare Connect">
        <meta name="robots" content="index, follow">

        <!-- Canonical URL -->
        <link rel="canonical" href="<%= canonicalUrl %>">

        <!-- Open Graph / Facebook -->
        <meta property="og:type" content="<%= typeof ogType !== 'undefined' ? ogType : 'website' %>">
        <meta property="og:url" content="<%= canonicalUrl %>">
        <meta property="og:title" content="<%= seoTitle %>">
        <meta property="og:description" content="<%= seoDescription %>">
        <meta property="og:image" content="<%= baseUrl + seoImage %>">
        <meta property="og:site_name" content="EduShare Connect">

        <!-- Twitter -->
        <meta name="twitter:card" content="summary_large_image">
        <meta name="twitter:url" content="<%= canonicalUrl %>">
        <meta name="twitter:title" content="<%= seoTitle %>">
        <meta name="twitter:description" content="<%= seoDescription %>">
        <meta name="twitter:image" content="<%= baseUrl + seoImage %>">

        <!-- Google Site Verification (if configured) -->
        <% if (process.env.GOOGLE_SITE_VERIFICATION) { %>
            <meta name="google-site-verification" content="<%= process.env.GOOGLE_SITE_VERIFICATION %>">
            <% } %>

                <!-- Favicon -->
                <link rel="icon" type="image/x-icon" href="/favicon.ico">

                <!-- Google Fonts -->
                <link rel="preconnect" href="https://fonts.googleapis.com">
                <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
                <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap"
                    rel="stylesheet">

                <!-- Stylesheets -->
                <link rel="stylesheet" href="/css/design-system.css">
                <link rel="stylesheet" href="/css/components.css">
                <link rel="stylesheet" href="/css/style.css?v=3">
                <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

                <!-- Structured Data (JSON-LD) -->
                <% if (typeof structuredData !=='undefined' ) { %>
                    <script type="application/ld+json">
        <%- structuredData %>
    </script>
                    <% } else { %>
                        <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "WebSite",
        "name": "EduShare Connect",
        "description": "Peer-to-peer educational platform for sharing knowledge and resources",
        "url": "<%= baseUrl %>",
        "potentialAction": {
            "@type": "SearchAction",
            "target": "<%= baseUrl %>/resources?search={search_term_string}",
            "query-input": "required name=search_term_string"
        }
    }
    </script>
                        <% } %>

                            <!-- Google AdSense -->
                            <script async
                                src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-4619809727765536"
                                crossorigin="anonymous"></script>
</head>

<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="container navbar-container">
            <a href="/" class="navbar-brand">📚 EduShare Connect</a>

            <button class="navbar-toggle" id="navbarToggle" aria-label="Toggle navigation">
                <span></span>
                <span></span>
                <span></span>
            </button>

            <ul class="navbar-menu" id="navbarMenu">
                <li class="navbar-item"><a href="/">🏠 Home</a></li>
                <li class="navbar-item"><a href="/resources">📖 Resources</a></li>
                <li class="navbar-item"><a href="/courses">🎓 Courses</a></li>
                <li class="navbar-item"><a href="/forums">💬 Forums</a></li>
                <li class="navbar-item"><a href="/reels">🎬 Reels</a></li>

                <% if (user) { %>
                    <li class="navbar-item"><a href="/favorites">❤️ Favorites</a></li>
                    <li class="navbar-item"><a href="/notes">📝 Notes</a></li>
                    <li class="navbar-item"><a href="/communication">✉️ Chat</a></li>
                    <li class="navbar-item"><a href="/gamification/leaderboard">🏆 Leaderboard</a></li>
                    <li class="navbar-item"><a href="/ai-assistant">🤖 AI Chat</a></li>

                    <% if (user.role==='teacher' || user.role==='admin' ) { %>
                        <li class="navbar-item"><a href="/resources/upload/new">⬆️ Upload</a></li>
                        <% } %>

                            <li class="navbar-item">
                                <div class="d-flex align-center gap-2">
                                    <span>👤 <%= user.username %></span>
                                    <a href="/logout" class="btn btn-sm btn-outline">Logout</a>
                                </div>
                            </li>
                            <% } else { %>
                                <li class="navbar-item"><a href="/login" class="btn btn-primary btn-sm">Login</a></li>
                                <li class="navbar-item"><a href="/register"
                                        class="btn btn-secondary btn-sm">Register</a></li>
                                <% } %>
            </ul>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container" style="padding-top: 2rem; padding-bottom: 2rem; min-height: 70vh;">
        <%- body %>
    </main>

    <!-- Footer -->
    <footer style="background: var(--gray-900); color: var(--text-light); padding: 3rem 0; margin-top: 4rem;">
        <div class="container">
            <div class="row stack-mobile">
                <div class="col-4 col-md-12 mb-4">
                    <h3 style="color: var(--text-light); margin-bottom: 1rem;">📚 EduShare Connect</h3>
                    <p style="color: var(--gray-400);">Interactive peer-to-peer educational platform for collaborative
                        learning.</p>
                </div>
                <div class="col-4 col-md-6 mb-3">
                    <h4 style="color: var(--text-light); margin-bottom: 1rem;">Quick Links</h4>
                    <ul style="list-style: none; padding: 0;">
                        <li style="margin-bottom: 0.5rem;"><a href="/resources"
                                style="color: var(--gray-400); text-decoration: none;">Resources</a></li>
                        <li style="margin-bottom: 0.5rem;"><a href="/courses"
                                style="color: var(--gray-400); text-decoration: none;">Courses</a></li>
                        <li style="margin-bottom: 0.5rem;"><a href="/forums"
                                style="color: var(--gray-400); text-decoration: none;">Forums</a></li>
                        <li style="margin-bottom: 0.5rem;"><a href="/reels"
                                style="color: var(--gray-400); text-decoration: none;">Educational Reels</a></li>
                    </ul>
                </div>
                <div class="col-4 col-md-6 mb-3">
                    <h4 style="color: var(--text-light); margin-bottom: 1rem;">Community</h4>
                    <ul style="list-style: none; padding: 0;">
                        <li style="margin-bottom: 0.5rem;"><a href="/gamification/leaderboard"
                                style="color: var(--gray-400); text-decoration: none;">Leaderboard</a></li>
                        <li style="margin-bottom: 0.5rem;"><a href="/ai-assistant"
                                style="color: var(--gray-400); text-decoration: none;">AI Assistant</a></li>
                        <li style="margin-bottom: 0.5rem;"><a href="/portal"
                                style="color: var(--gray-400); text-decoration: none;">Student Portal</a></li>
                    </ul>
                </div>
            </div>
            <div
                style="text-align: center; margin-top: 2rem; padding-top: 2rem; border-top: 1px solid var(--gray-800);">
                <p style="color: var(--gray-500);">&copy; <span id="layout-footer-year"></span> EduShare Connect. All
                    rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/search.js"></script>
    <script src="/js/reviews.js"></script>
    <script src="/js/favorites.js"></script>
    <script src="/js/notes.js"></script>
    <script src="/js/forums.js"></script>
    <script>
        // Set dynamic copyright year
        const yearElement = document.getElementById('layout-footer-year');
        if (yearElement) {
            yearElement.textContent = new Date().getFullYear();
        }

        // Mobile navigation toggle
        document.getElementById('navbarToggle')?.addEventListener('click', function () {
            this.classList.toggle('active');
            document.getElementById('navbarMenu')?.classList.toggle('active');
        });

        // Close mobile menu when clicking outside
        document.addEventListener('click', function (e) {
            const navbar = document.getElementById('navbarMenu');
            const toggle = document.getElementById('navbarToggle');

            if (navbar && toggle && !navbar.contains(e.target) && !toggle.contains(e.target)) {
                navbar.classList.remove('active');
                toggle.classList.remove('active');
            }
        });
    </script>
</body>

</html>